# 設計ドキュメント

このテンプレートの設計思想と規約をまとめたものです。新しく触る人はここから読んでください。

## 目次

- [architecture.md](./architecture.md) — ディレクトリ構成・データフロー・「どこに何を置くか」の規約
- [data-access.md](./data-access.md) — Supabase アクセス層（型安全エンジン・スキーマ・embed・適用範囲）
- [adding-a-resource.md](./adding-a-resource.md) — 新しいリソースを足す手順

## 核となる設計思想

> **スキーマを単一の真実とし、そこから型・検証・経路を派生させる。**

具体的には次の 4 つを徹底しています。

1. **1 スキーマが 3 役を兼ねる**
   リソースの zod スキーマ 1 つから、①実行時バリデーション ②戻り値の型 ③filter のカラム型 がすべて派生する。定義は 1 箇所、二重定義は構造的に禁止。

2. **端から端まで型で閉じる**
   操作キー（`@select/todos`）・filter のカラム名と値・戻り値、すべてコンパイル時に検証される。

3. **エラーは値として扱う**
   データ層は例外を投げず `Result`（neverthrow）を返す。境界（serverFn）で明示的に処理する。

4. **経路を一本化する**
   読み取りも書き込みも serverFn 経由。検証・認可をサーバーに集約し、ブラウザからの直叩きは Realtime 等の明示的オプトインだけに限定する。

## 適用範囲（重要）

このアクセス層は **単一テーブルの CRUD ＋ 関連データの読み取り（embed）** を型安全にカバーします。
関連テーブルの列での型付き filter/order（postgrest の `referencedTable`）は未対応で、必要なときは素のクライアント（`$supabaseServer().raw`）に退避します。詳細は [data-access.md](./data-access.md#適用範囲と限界) を参照。
