# 環境変数の定義・バリデーションの正本は src/env.ts（t3-env）。
# クライアント公開変数は VITE_ 接頭辞が必須。

# --- 必須 ---
# Supabase プロジェクトの URL と公開（publishable / anon）キー
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# --- 任意 ---
# アプリ名（未設定可）
VITE_APP_TITLE=
# サーバー URL（未設定可）
SERVER_URL=
